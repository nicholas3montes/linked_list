
<div class="row">.</div>
<div class="h-25 d-inline-block">    
  <table class="table table-dark table-hover">
    <thead>
      <tr>
        <th><div class="text-primary ">Criar Lista</div></th>

      </tr>
    </thead>
    <tbody>
    <%= form_for(@text, remote: true) do |f| %>
      <tr>
        <td><%= f.text_field :text%><br /></td>
        <td><%= f.submit 'Incluir', class: 'btn btn-dark' %></td>
      </tr>
      <tr>
      </tr>
    <% end %>
    </tbody>
  </table>
  <p class="badge badge-dark">Insira os itens separados por virgulas.</p>
</div>